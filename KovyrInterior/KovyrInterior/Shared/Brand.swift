import SwiftUI

/// Kovyr brand palette. The primary accent is Kovyr navy (#1E3A5F), matching
/// the external product's `brand.json`.
extension Color {
    /// Kovyr navy — primary brand/accent color (#1E3A5F).
    static let kovyr = Color(red: 30 / 255, green: 58 / 255, blue: 95 / 255)

    /// A lighter steel-blue used for highlights and gradients.
    static let kovyrAccent = Color(red: 74 / 255, green: 122 / 255, blue: 178 / 255)
}

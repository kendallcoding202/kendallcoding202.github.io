# This branch: Solana meme coin tracker

Before any work in `pumpbot/` or `solana-tracker/`, read **[SOLANA-HANDOFF.md](SOLANA-HANDOFF.md)**.

Cursor may be working in parallel on branch `cursor/solana-tracker`. Do not push to that branch. Do not force-push this branch (`claude/solana-meme-coin-tracker-gugfaj`). Fetch and merge Cursor's branch if it has commits you do not have.

This `CLAUDE.md` is for this feature branch only. Do not copy it onto `main` as a repo-wide rule.
